export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
  /* Creates a new promise OBJECT, passes two functions (resolve, reject)
  /  resolve -> call this when async work is successful
  /  reject -> call this when async work fails */
   });
}
